package com.kodilla.sudoku;

public class SudokuGame {

    public boolean resolveSudoku() {
        /*
         if(user want to game again)
         should return "true"

         if(user want to finish)
         should return false
        */
        return true;
    }
}
